# pt-tools — 品牌资源

> English version: [README.md](./README.md)

pt-tools 的视觉标识：一个主标志、一个单色版本、一个横向组合锁定（字标 + 图形），
以及一整套 favicon / 应用图标尺寸阶梯。

这里所有文件都由 `build.py` 从同一份参数化的标志定义生成，所以矢量源文件和位图
不可能互相跑偏。要改标志，改参数然后重新生成：

```sh
uv run --with fonttools --with cairosvg --with pillow python3 build.py
```

不要手工编辑生成出来的 SVG，下一次构建会直接覆盖掉。

## 标志的含义

<!-- 本节仅描述当前版本标志的语义。标志形态如有调整，只需替换本节。 -->

一个 shell 提示符的尖括号，后面跟着等待输入的光标方块：

| 元素 | 颜色 | 含义 |
| --- | --- | --- |
| 尖括号 | teal | 提示符——工具在等你 |
| 后面那个方块 | orange | 光标，你的命令落在这里 |
| 两者同一条轴线，中间留缝 | — | 命令行上的两个 token，不是一个粘在一起的形状 |

**pt-tools 是一个 Go 命令行工具，这个标志只说这一件事。**它画的就是你还没敲字时，
终端那一行上真实存在的两样东西。69 个画出来的方案里，只有这一个，外行人的第一直觉
读法和它想表达的意思是对上的：把图标单独给人看，得到的回答是终端、命令行工具。

它刻意**不是**图表，也刻意**不是**下载箭头。早先有一版是从左往右升上去的，所有人
都读成柱状图；pt-tools 会报数字，但它不是仪表盘，它会路由和过滤，但它不是下载器。

### 为什么是提示符，而不是某个更聪明的东西

在定下这一版之前画了并实测了另外 68 个方案。其中三条结论值得留下来，否则以后一定
会被重新踩一遍：

- **平涂方块拼图（画了 24 个）全都被认成了别的东西**——楼梯、柱状图、餐叉、栅栏。
  它们还撞上一个死结：一块填满的网格要读成网格，就得看见格子之间的分隔，但同色的
  相邻格子会融成一整块，而格子之间留缝在 16 px 下会被抗锯齿填满。
- **节点加环的那一类（画了 12 个）读起来是原子**，而三节点的那一版还跟姊妹产品
  CodeGraph 的标志撞了——后者本来就是一张三节点图。
- **字母造型 `pt` 在图标尺寸下做不出来。**在 48 单位的方框里，p 的字碗和 t 的横杠
  会互相撞上；把它们撑开到足以分离，两根笔画就都细到过不了 favicon 了。

还有一个后期方案，把语义寄托在一段用透明度淡化的回折线段上。实测在 16 px 下，那段
淡化的线段直接没了——这就是下面那条「不要用透明度」为什么不是审美偏好。

## 文件

| 文件 | 用在哪 |
| --- | --- |
| `logo.svg` | 主标志。自带底板，所以放在任何背景上都能用，不需要做主题适配。没有特别理由就从这个开始。 |
| `logo-mono.svg` | 用 `currentColor` 实现的单色版本。内联进 HTML / Markdown 就会继承周围文字的颜色——深底浅字、浅底深字自动成立。底板不合适的场合用它。 |
| `wordmark.svg` | 图形 + 名称的组合锁定，1241 × 320。用于 README 页头、文档横幅、幻灯片标题。文字已转成轮廓，所以在没装字体的机器上渲染结果也一样。 |
| `icon-512.png` | GitHub 仓库社交预览图、Docker Hub 头像、应用图标源文件、README 头图。 |
| `icon-256.png` | 桌面应用图标、包仓库头像。 |
| `icon-128.png` | 文档侧边栏、中等尺寸界面。 |
| `icon-64.png` | 小尺寸界面、列表行。 |
| `icon-32.png` | 标准 favicon。 |
| `icon-16.png` | 浏览器标签页 favicon，可辨识的最小尺寸。 |

所有 PNG 都是带真实透明通道的 RGBA，每个尺寸都是从 `logo.svg` 按目标尺寸原生
渲染出来的，不是从最大那张缩下来的，所以每一张在自己的像素预算下都是锐利的。

`build.py` 还会写出 `fonts.conf`。这个文件存在的原因是：cairo 会继承宿主机
fontconfig 的 LCD 次像素抗锯齿设置，那会把带颜色的边缘条纹烤进位图里，让输出结果
取决于是谁的机器构建的。所以在 cairo 加载之前就强制成灰度抗锯齿，这套图标阶梯
才能在任何地方复现。这一点是实测过的：分别在 `rgba=none` 和 `rgba=rgb` 下渲染
`logo.svg` 再逐字节比对，所有尺寸的输出都完全一致。

### 建议的对应关系

| 用途 | 文件 |
| --- | --- |
| GitHub 社交预览（1280 × 640 画布） | `icon-512.png`，居中 |
| GitHub README 页头 | `wordmark.svg` |
| Favicon | `logo.svg`，用 `icon-32.png` / `icon-16.png` 作降级 |
| Docker Hub / 包仓库头像 | `icon-512.png` |
| 桌面应用图标 | `icon-512.png` → 各平台的图标打包工具 |
| 正文内联、徽章、终端相关文档 | `logo-mono.svg` |

## 颜色

四个值。除此之外的颜色都不属于这个标志。

| 名称 | 十六进制 | 角色 |
| --- | --- | --- |
| Ink | `#0B1220` | 底板、字标的色块 |
| Paper | `#E7EDF5` | 只用于字标文字——图标里不出现 |
| Brand orange | `#F97316` | 光标方块，仅此一处 |
| Teal | `#14B8A6` | 提示符尖括号 |

**paper 已经不出现在图标里了。**图标是底板加两种颜色，不是三种。这是刻意的简化，
不是漏了：整个标志只有尖括号和光标两个元素，再多一种颜色就得硬给它编一个职责。
paper 剩下的职责是字标的文字——那是这套调色板里唯一一处需要被**读出来**、而不只是
被看见的地方。

**橙色在 paper 上实测对比度是 2.7:1。**它过不了文字对比度要求，所以只允许用作
图形块面——绝不用于正文、绝不用于细线、绝不用于任何需要被读出来的细节。这一组里
唯一能过文字对比度的搭配是 ink 配 paper，这也是为什么字标是在 ink 色块上放
paper 色的字，而不是在透明背景上放 ink 色的字：色块让整个组合锁定留在调色板范围
内，同时在任何背景上都可读，包括那些会让裸 ink 文字直接消失的深色背景。

## 安全边距

四边最小留白都是**标志高度的 25%**。任何东西——文字、分割线、其他 logo、容器
边缘——都不许进入这条带子。

`logo.svg` 在底板内部已经自带了一层边距（画布的 12.5%），所以 25% 这条规则是从
底板边缘量起的，不是从图形本身量起。

## 最小尺寸

| 资源 | 最小尺寸 |
| --- | --- |
| `logo.svg` / `logo-mono.svg` | 16 px |
| `wordmark.svg` | 宽 96 px（再小文字就糊成一团） |

标志建在 1024 画布内的 64 单位网格上。1024 ÷ 64 = 16，所以在 16 px 的 favicon 上
每一条**正交**边都正好落在整像素上——光标方块正好栅格化成 5 × 4 个整像素。
**必须守住网格**：任何一条边偏离 64 单位的整数倍，都会在小尺寸下重新引入次像素模糊。

尖括号是一段 45° 的圆头笔画，所以跟上一版标志不同，它**天生就带抗锯齿**。这是走
斜线要付的代价，而且是明知故付；「图形部分零抗锯齿像素」这个性质在描边设计下不成立，
所以这条说法是被撤掉的，不是被含混地保留。

下面这组数据是在 `icon-16.png` 上实测的，不是嘴上说的：**尖括号有 22 个完全不透明
的像素，光标有 20 个完全不透明的像素，没有任何一个被冲淡的像素，两者之间是干净的
2 px 缝隙。**笔画粗细就是靠这组证据定下来的——降到 1.5 单位时，尖括号退化成 8 个
不透明像素加 12 个被冲淡的像素，那是一团灰糊，不是一个提示符；加到 2.5 单位时，
16 px 的栅格化结果和 2 单位完全一样。所以 2 单位既是下限，也就是答案。这里如实记一笔：
这个标志所依据的候选草图，笔画是 48 单位方框里的 2.6，折算到 16 px 的 favicon 上只有
0.87 px，那是撑不住的。

如实说明它的边界：在 16 px 下，把图标单独给人看，对方会看到一撮 teal 色的楔形和一块
橙色方块，而不会说出「大于号」——几乎所有 favicon 都是这样。标志在这个尺寸的职责是
成为一个可分辨的双元素识别特征，这一点它做到了。语义读法从 32 px 开始成立，在那个
尺寸下尖括号实测有 72 个实心像素，毫无歧义。

## 禁止事项

- **不要改色**，超出上面那几个值。特别是橙色必须留在光标上：橙色在这里是图形块面，
  永远不是需要被读出来的细节，一个 2.7:1 的颜色撑不起尖括号。
- **不要把尖括号的笔画压到 2 个单位以下**（即 16 px favicon 上的 2 px）。整个标志的
  可辨识度就压在这一个数上，见「最小尺寸」。
- **不要用透明度**去做层次或纵深。淡化过的元素在 16 px 下会退化成幽灵，并且会改变
  标志表达的意思，有一个后期方案正是因此被否掉的。这里的层次只来自块面和位置。
- **不要把尖括号和光标之间的缝隙合上。**它是 2 个单位，而且它就是语义本身——这是
  命令行上两个各自独立的 token。缝隙缩到 1 个单位，在 16 px 下就会被抗锯齿填满，
  两个元素会读成一个形状。注意这一条跟姊妹标志「各部分必须相贴」的规则是反的：那条
  规则是为平涂拼图推出来的，在那种造型里缝隙是唯一的失效方式。
- **不要把光标加宽到超过 5 个单位。**它跟右侧墨迹边缘齐平，加宽就会吃掉缝隙。为了
  换更长的尖括号而把它收窄，是实测过并被否掉的——那会把标志的比例关系倒过来，
  平衡也更差。
- **不要拉伸。**等比缩放。标志是正方形，字标是 1241 × 320，必须保持这个比例。
- **不要加效果**——不要投影、外发光、渐变、斜面或轮廓。尖括号的描边是几何本身，
  不是效果；除它以外任何元素都不许有描边。
- **不要把尖括号的端头改成方头。**圆端头加圆转角，才让它读起来像一个敲出来的字符，
  而不是一个箭头，也不是从底板上切出来的人字形。
- **不要用活字体去排字标。**用 `wordmark.svg`，它的文字已经是轮廓了。在 `<text>`
  元素里重新打一遍 "pt-tools"，会导致每台机器渲染出来都不一样。
- **不要旋转或镜像。**尖括号朝右，跟提示符一样。
- **不要把 `logo-mono.svg` 放在中间调背景上**而不先检查对比度——它只有一种颜色，
  跟背景之间没有任何内建的分离度。

## 命名

产品名是 **`pt-tools`**——全小写，带连字符。这是包名。不是 "PT-Tools"，不是
"PT Tools"，也不是 "pttools"。

## 家族

pt-tools 是共用同一套造型语言的四个产品标志之一：1024 画布、硬转角、不用渐变、
不用透明度，以及这套调色板。另外三个是 AgentLens（切面六边形）、Voxera（分裂
人字形）和 CodeGraph（三节点图）。新增资源请留在这套语言里，整组才不会散。

共享的那条规则是：**少量大块面，内部不做琐碎细节**——本标志是两个元素，三个姊妹
各是三个。给 pt-tools 加第三个元素，或者引入一个会被单色抹掉的内部细节，会同时
破坏家族相似性和 16 px 下限这两件事。

有一处是刻意的分歧：三个姊妹标志是用颜色边界分割块面、各部分相贴，而 pt-tools 中间
是一条实打实的 2 单位缝隙。那条规则是为平涂拼图推出来的，在那种造型里缝隙是唯一的
失效方式。而这里缝隙承载语义——命令行上的两个 token——所以它的尺寸是按「撑得住
抗锯齿」定的，而不是被取消掉。造型语言的其余部分都成立，包括那条缝隙规则真正想
保护的约束：**不要留会被单色抹掉的内部负形**。这也正是 `logo-mono.svg` 仍然成立的
原因。

## 许可

这些资源属于 pt-tools 项目，除另有说明外沿用同一许可（MIT）。
